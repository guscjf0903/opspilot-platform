apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-api
  namespace: sample-app
spec:
  template:
    spec:
      containers:
        - name: payment-api
          readinessProbe:
            httpGet:
              path: /missing-health
              port: http
